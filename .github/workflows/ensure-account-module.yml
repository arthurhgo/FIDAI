name: Ensure FIDAI account and auth modules

on:
  push:
    branches:
      - main
      - agent/alinhar-pedidos-pix-admin
    paths:
      - assets/fidai-account.js
      - assets/fidai-auth-redirect.js
      - .github/workflows/ensure-account-module.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  inject-account-module:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.ref_name }}

      - name: Load auth redirect and account manager in the site
        run: |
          python - <<'PY'
          from pathlib import Path

          path = Path('index.html')
          html = path.read_text(encoding='utf-8')
          auth_tag = '<script defer src="./assets/fidai-auth-redirect.js"></script>'
          account_tag = '<script defer src="./assets/fidai-account.js"></script>'
          orders_tag = '<script defer src="./assets/fidai-orders.js"></script>'

          tags = [auth_tag, account_tag]
          for tag in tags:
              if tag in html:
                  continue
              if orders_tag in html:
                  html = html.replace(orders_tag, f'{tag}{orders_tag}')
              else:
                  html = html.replace('</body>', f'{tag}</body>')

          path.write_text(html, encoding='utf-8')
          PY

      - name: Commit generated index
        run: |
          if git diff --quiet; then
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html
          git commit -m "Load FIDAI auth and account modules"
          git push origin HEAD:${{ github.ref_name }}
